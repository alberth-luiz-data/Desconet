runConfig:
  minInstances: 0  # Desliga quando não há tráfego (economiza custos)
  # maxInstances: 100  # Comentado = usa padrão do Cloud Run
  # concurrency: 80    # Comentado = usa padrão (80 requisições/instância)
  # cpu: 1             # Comentado = 1 vCPU padrão
  # memoryMiB: 512     # Comentado = 512MB padrão